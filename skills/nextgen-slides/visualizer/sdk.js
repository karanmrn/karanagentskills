// slides-viz SDK — intentionally a no-op.
//
// The deck is served same-origin inside the visualizer iframe, so the editor
// shell (ui/editor-core.js) drives the deck's document directly — hover
// highlight, click-to-comment, selector computation, and navigation live there.
// This file stays injected (via html-transform) only as a stable hook point.
void 0;
